script "script_enrich_email" {
  source `
    {
      "to": input["to"],
      "subject": "[script] " + input["subject"],
      "body": input["body"],
      "enriched": true,
      "app_env": input["app_env"],
      "tenant_config": input["tenant_config"]
    };
  `
}
