script "script_enrich_email" {
  source `
    let req = input;
    {
      "to": req.to,
      "subject": "[script] " + req.subject,
      "body": req.body,
      "enriched": true,
      "app_env": req.app_env,
      "tenant_config": req.tenant_config
    };
  `
}
